# @package _global_

defaults:
  - override /model: zephyr-7b-beta
  - override /trainer: SimNPO
  - override /data: unlearn
  - override /data/datasets@data.forget: WMDP_forget
  - override /data/datasets@data.retain: WMDP_retain
  - override /eval: lm_eval

data_split: bio

data:
  anchor: forget
  forget:
    WMDP_forget: 
      args:
        hf_args:
          data_files: data/wmdp/wmdp-corpora/${data_split}-forget-corpus.jsonl
  retain:
    WMDP_retain:
      args:
        hf_args:
          data_files: data/wmdp/wmdp-corpora/${data_split}-retain-corpus.jsonl

eval:
  lm_eval:
    tasks:
      - wmdp_${data_split}
      - mmlu

collator:
  DataCollatorForSupervisedDataset:
    args:
      padding_side: left # Usually left but for mistral and zephyr its right (https://github.com/hongshi97/CAD/issues/2)

trainer:
  args:
    per_device_train_batch_size: 1
    gradient_accumulation_steps: 16
    learning_rate: 1e-5
    max_steps: 100
    warmup_ratio: 0.1

  method_args:
    delta: 0.1 # gamma in https://github.com/OPTML-Group/Unlearn-Simple/blob/main/MUSE/README.md, default 0.1
    alpha: 0.1  # coeff in https://github.com/OPTML-Group/Unlearn-Simple/blob/main/MUSE/README.md
    gamma: 1.0  # npo_coeff in https://github.com/OPTML-Group/Unlearn-Simple/blob/main/MUSE/README.md
    beta: 0.7

task_name: ???