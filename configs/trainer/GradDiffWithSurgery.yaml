defaults:
  - GradDiff

handler: GradDiffWithSurgery
method_args:
  # Gradient Surgery parameters (GradDiff parameters are inherited)
  surgery_strategy: "sago"  # ["sago", "sago_prefer_retain", "pcgrad", "pcgrad_global", "sum"]